/*
  1. Start a program to work with points, discussed in §10.4. Begin by defining the
     data type Point that has two coordinate members x and y.
*/
#include "../std_lib_facilities.h"

struct Point {
  int x, y;
};

int main()
{

  return 0;
}
